const mutations = {
	GET_FILMDATA(state, filmData) {
		state.filmData = filmData
	},
	GET_ITEMFILE(state, itemFile) {
		state.itemFile = itemFile
	},
	GET_CHANGE(state, change) {
		state.change = change
	}
}

export default mutations